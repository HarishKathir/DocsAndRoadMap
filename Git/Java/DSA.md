# Arrays 
## 1D Array
### 1672 - Richest customer wealth
- you are given a mxn array
- each sub array is the amount ith person holds in jth bank 
- Simply iterate through the array and find the sum of each person and return the max 

### 1 - Two Sum
- you are given a array of n number and a target
- iterate through the array 
- init a hashmap which stores the number and the index
- find the diff between the target and the current element
- if the diff number is presnt in the map , return the value of the map and the index
- else add the element from the array and the index of the element to the map

### 351 - Count Negative number in the sorted matrix 
- Simple brute force , iterayte through the array